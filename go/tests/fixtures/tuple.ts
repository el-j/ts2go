// Tuple examples
type Pair = [string, number];
type Triple = [string, number, boolean];

// Simple function that just returns a string
function getMessage(): string {
  return "Tuples defined successfully";
}

console.log(getMessage());